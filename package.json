{
  "name": "expiry-ledger-complete-medicine",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "临期账本：药品效期管理，EdgeOne Pages + KV + 七牛云 + 微信通知",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/postbuild.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "html5-qrcode": "^2.3.8",
    "vite": "^6.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.18.0"
  }
}

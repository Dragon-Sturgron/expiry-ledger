{
  "name": "expiry-ledger-complete-medicine",
  "version": "2.7.0",
  "private": true,
  "type": "module",
  "description": "临期账本：药品效期管理，首页状态卡联动筛选 + 新版临期记录表单 + EdgeOne KV",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/postbuild.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "html5-qrcode": "^2.3.8",
    "vite": "^6.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.18.0"
  }
}
